# Zone that hosts the CV records, e.g. osmarpetry.dev
DNS_CV_DOMAIN=osmarpetry.dev

# Cloudflare API token scoped to that single zone, with Zone > DNS > Edit only.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=

# Optional
DNS_CV_TTL=3600
DNS_CV_MAX_BYTES=2048
